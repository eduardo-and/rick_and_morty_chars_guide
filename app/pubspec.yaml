name: rick_and_morty_guide
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: 3.0.1
  mobx: 2.0.7
  flutter_mobx: 2.0.6+4
  dio: 4.0.6
  get_it: 7.2.0
  shared_preferences: 2.0.15
  flutter_svg: 1.1.5
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.11
  mobx_codegen: ^2.0.7
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo.svg
